import { MapAgreeComponent } from './mapAgree.component';

describe('mapAgreeModule', () => {
  let mapAgreeModule: MapAgreeComponent;

  beforeEach(() => {
    mapAgreeModule = new MapAgreeComponent();
  });

  it('should create an instance', () => {
    expect(mapAgreeModule).toBeTruthy();
  });
});
